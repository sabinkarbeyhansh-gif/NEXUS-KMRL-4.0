import { en } from './en';
import { ml } from './ml';
import { hi } from './hi';
import { LanguageCode } from '../types';

export const translations = {
  en,
  ml,
  hi,
};

export type TranslationKey = typeof en;
